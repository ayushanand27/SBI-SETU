# Alternative: Railway deploy — https://railway.app
# Backend service only; host frontend on Render static or Vercel.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
